BOOTARGS
The kernel command line.

Use make after configuration and the boot wrapper


 link the kernel and its configuration together into a file

 ready for the model to use, typically linux-system.axf.
